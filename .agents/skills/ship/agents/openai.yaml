interface:
  display_name: "Ship Excelsior"
  short_description: "Validate, deploy, and verify Excelsior releases"
  default_prompt: "Use $ship to validate the approved changes, commit and push them, follow deployment, and verify production health."
