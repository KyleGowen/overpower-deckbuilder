/**
 * Shared deck validation constants (STD / Venture rules).
 * Loaded before deck-validation.js and inline index scripts.
 */
// eslint-disable-next-line no-var -- intentional global for legacy scripts
var DECK_RULES = {
    MIN_DECK_SIZE: 51,
    MIN_DECK_SIZE_WITH_EVENTS: 56,
    EXACT_CHARACTERS: 4,
    MAX_TOTAL_THREAT: 76,
    MAX_COPIES_ONE_PER_DECK: 1,
    EXACT_MISSION_CARDS: 7,
    MAX_LOCATIONS: 1
};
